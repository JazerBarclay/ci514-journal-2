/* 
 * Assembly Two Way Traffic Lights
 * 
 * Like the timer overflow interrupt we set
 * the counter and decrement it every interrupt.
 * 
 * When the counter hits 0 we have the zero
 * flag set. With this we can execute some 
 * subroutine that happens after a certain 
 * number of interrupts.
 * 
 * Author: Jazer Barclay
 */
