/* 
 * Assembly Compare
 * 
 * Compare numbers to see which is bigger
 * and turns of the on-board led if
 * the first is bigger than the second
 * 
 * If the values are equal then the led
 * will remain off.
 * 
 * The code shows where to add an equal 
 * operation could be added.
 * 
 * Author: Jazer
 */
