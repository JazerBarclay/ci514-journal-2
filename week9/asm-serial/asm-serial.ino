/* 
 * Assembly Serial
 * 
 * I created a simple sketch that uses the 5 i/o registers
 * associated with serial communication to transmit the
 * character 'A' (ASCII 65) with a 1 second delay between
 * each tramission.
 * 
 * UDR - USART Data Register
 * UCSR[A,B,C] USART Control Status Register
 * UBRR - USART Baud Rate Register
 * 
 * Author: Jazer
 */
